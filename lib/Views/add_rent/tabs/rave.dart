import 'package:flutter/material.dart';

class Rave extends StatelessWidget {
  const Rave({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
